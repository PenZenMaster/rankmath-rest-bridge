{
  "name": "RankRocket SEO Control Layer",
  "slug": "rankmath-rest-bridge",
  "version": "2.11.3",
  "download_url": "https://raw.githubusercontent.com/PenZenMaster/rankmath-rest-bridge/main/releases/v2.11.3/rankmath-rest-bridge.zip",
  "author": "Rank Rocket Co.",
  "author_homepage": "https://rankrocket.co",
  "homepage": "https://rankrocket.co",
  "requires": "5.9",
  "requires_php": "7.4",
  "tested": "6.7",
  "sections": {
    "description": "Native SEO control layer for the RankRocket remediation pipeline. Manages title/meta, schema injection, image ALT text, llms.txt, XML sitemap, robots.txt, cache purge, and self-updates.",
    "changelog": "v2.11.3: Canonical URL emission to <head> for singular allowed-post-type pages, with per-post _rr_seo_canonical override field on /update, /preview-update, /meta/bulk-update, and /get/{id} (filters: rrseo_canonical_url, rrseo_emit_canonical). Twitter Card emission (twitter:card, twitter:title, twitter:description, twitter:image) with per-post _rr_seo_twitter_* overrides falling back to OG fields, then post defaults. robots.txt auto-sync: when no custom robots.txt is stored, the dynamic robots_txt filter strips Sitemap: /wp-sitemap.xml lines and appends Sitemap: /sitemap_index.xml so robots.txt agrees with the active sitemap (gated by rrseo_robots_txt_auto_sync, default on). wp_robots() consolidation: per-post rr_seo_robots directives are merged into WordPress core's single <meta name=\"robots\"> tag instead of emitting a second tag, preserving max-image-preview:large (gated by rrseo_consolidate_wp_robots, default on). /status now reports robots_txt_auto_sync and consolidate_wp_robots flags.\nv2.11.2: Fix robots.txt read-side stale state — POST /robots-txt now busts WP object cache after update_option so GET /robots-txt returns the live value instead of a cached empty string; clearstatcache added to GET and POST file_exists calls; GET /status bypass warning now suppressed when the plugin is managing the physical file.\nv2.11.1: Fix snippet renderer ignoring display_on:all — switch statement handled 'entire_website' but not 'all' (the value RankRocket sends), causing sitewide snippets to silently not render. Added 'all' as the primary case plus aliases 'home'/'homepage' for front_page.\nv2.11.0: robots.txt now written as a physical file on every POST /robots-txt save — changes persist if the plugin is ever deactivated. Activation hook syncs existing DB content to the physical file on upgrade. Deactivation confirmation dialog added to the Plugins screen listing which features stop vs persist. GET /robots-txt adds physical_file_managed field.\nv2.10.1: Fix check-updates handler for private plugins — remove delete_site_transient (no WordPress.org contact), store PUC checker globally, call checkForUpdates() directly against private manifest, return version result to UI. Rename button to 'Check for Updates'.\nv2.10.0: AEO/GEO audit data layer — five new REST endpoints: GET /canonical-urls/preview (join spine for audit engine), GET /aeo-geo/readiness (top-level snapshot with scoring), GET /aeo-geo/entity (NAP and entity signals), GET /aeo-geo/schema-audit (per-URL schema type inventory), GET /aeo-geo/source-sync (canonical vs sitemap vs llms.txt three-way diff). Resolves P2 backlog gap for /canonical-urls/preview.\nv2.9.3: Fix schema JSON-LD visible as plain text in footer (wp_kses_post stripped script tags from managed snippets); fix PUC vendor path mismatch that silently disabled auto-update notifications.\nv2.9.2: Legacy rankmath-bridge/v1 namespace alias via rest_pre_dispatch proxy; register_post_meta for _rrseo_llms_section (show_in_rest, sanitize, auth); fix first-paragraph description fallback bug where content was normalized before paragraph splitting.\nv2.9.1: Force Update Check button + POST /check-updates; admin llms.txt config tab rebuilt.\nv2.9.0: Section classifier, /llms/preview, _rrseo_llms_section meta."
  }
}
