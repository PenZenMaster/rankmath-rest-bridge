# CheckPoint 2026-07-20_1840 -- Shutdown: v3.4.0 + v3.4.1, issues #9/#10/#11 shipped same-day

## Context Summary

Shutdown checkpoint for the 2026-07-20 session. Two new field-surfaced
issues (#9, #10) arrived from the Kilday Baxter & Associates SEO audit and
were bundled into a single v3.4.0 AEO/GEO write-surface release. A replay
of the same audit against the freshly-deployed v3.4.0 immediately surfaced
a regression (#11) in the fix itself -- caught and shipped same-day as
v3.4.1.

## Accomplishments (whole session)

1. **v3.4.0 SHIPPED (issues #9, #10)** -- `POST /llms` `business_facts`
   writes are validated (`business_name` + `description` required, size
   caps, `422 invalid_business_facts`) instead of silently accepted and
   dropped; the Business Facts + Common Questions block now renders into
   `/llms.txt` by default instead of requiring a hand-authored `sections`
   config; new fields `description`, `tagline`, `hours`,
   `years_in_business`, `key_differentiators`, `common_questions`;
   `aeo-geo/readiness`'s `has_business_facts` tightened to require
   `business_name` + >=2 enrichment fields, new `business_facts_source`
   signal; README gained full `POST /llms` + `/aeo-geo/*` documentation
   (previously undocumented). 16 new tests (207 -> 223).
2. **v3.4.1 SHIPPED (issue #11)** -- same-day regression fix: partial
   `business_facts` writes were replacing the whole stored object instead
   of merging, contradicting the v3.4.0 README's own "omitted fields
   unchanged" contract and silently collapsing readiness scores on any
   incremental update. New `rr_merge_llms_business_facts()` merges
   key-by-key (array fields replace wholesale, not append); validation now
   runs against the merged result. 5 new tests (223 -> 228).
3. **Repo cleanup** -- removed 9 superseded release zips (v2.17.4-v3.2.0)
   at user's confirmation; only current + immediately-prior zips retained
   going forward.
4. Both releases: phpcs clean, full suite green, pre-push hook auto-built
   and verified both zips (4/4 structural checks each).

## Technical Changes

| File | Change |
|---|---|
| `rankmath-rest-bridge.php` | `rmb_llms_set_config()`: validates + merges `business_facts`; version 3.3.0 -> 3.4.1 |
| `includes/class-rrseo-llms.php` | New `rr_validate_llms_business_facts()`, `rr_merge_llms_business_facts()`, `rr_render_business_facts_lines()`; renderer now emits Business Facts by default |
| `includes/class-rrseo-aeo-geo.php` | `has_business_facts` rubric tightened (name + >=2 enrichment fields); new `business_facts_source` signal |
| `tests/unit/LlmsBusinessFactsTest.php` | New file: 17 tests (validator, merge, renderer) |
| `tests/unit/AeoGeoReadinessTest.php` | 4 new tests + 2 fixture updates for the tightened `has_business_facts` rubric |
| `README.md` | `POST /llms` payload shape + `business_facts` merge contract; new AEO/GEO section (4 endpoints + full scoring rubric) |
| `CHANGELOG.md`, `update-manifest.json` | v3.4.0 and v3.4.1 entries |
| `releases/v2.17.4-v3.2.0/` | Deleted (superseded, user-confirmed) |
| `releases/v3.4.0/`, `releases/v3.4.1/` | Zips (hook-built, 4/4 checks each) |

Commits this session: f8757ec, 9a20e97, fa6ce98, 9f73070, 540fdd8.

## Known Issues / Blockers

- None. All gates green (phpcs, 228/? phpunit assertions, suite 228 tests).
- v3.4.1 not yet deployed to any live site -- user is smoke-testing next.
- v3.3.0 -> v3.4.1 upgrade path on Higgins still pending (separate from
  this session's Kilday Baxter work); Higgins perf deployment (v3.3.0)
  from the prior session also still outstanding.
- Docs-only follow-up flagged in issue #11 (non-blocking): `entity_clarity`
  scoring is capped when manual `business_facts` omits `schema_type`/
  `entity_id`, because manual config fully overrides schema-derived
  signals by design -- worth a README callout, not filed as an issue yet.

## Next Session Priorities

1. User smoke-tests v3.4.1 (merge behavior + readiness scoring) against
   Kilday Baxter or another live site; confirm issue #11 regression
   test passes for real.
2. Close out issues #9, #10, #11 on GitHub once smoke test confirms.
3. Resume the still-pending Higgins v3.3.0 perf deployment (render-block
   swap, code_b64 + priority:1, PageSpeed measurement) -- carried over
   from before this session's audit-driven detour.

## Backlog Movement

- Closed (pending user confirmation post-smoke-test): issues #9, #10, #11
- Added same day: v3.4.0 (AEO/GEO write surface), v3.4.1 (merge fix)
- Unchanged / still carried: Higgins v3.3.0 deployment, telemetry verdict
  review, llms.txt raw-content verification, stale bite checkboxes in
  projectStatus.md
- Docs-only idea noted, not filed: entity_clarity rubric README callout
  re: manual business_facts overriding schema signals

## Git Status

- Branch: `main`, in sync with origin
- Last commit at checkpoint: `540fdd8`
- Pushed: yes
