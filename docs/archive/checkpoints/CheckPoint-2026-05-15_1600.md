# Checkpoint — 2026-05-15_1600

## Context Summary

Major sprint session. Starting from a clean v2.14.3 state, worked top-to-bottom
through the full open gap list. Shipped nine versions (v2.14.4 through v2.17.0),
closing every item from the original gap report except the deferred G-15 (hreflang).
Images polish bundle (GET /images/{id}/alt + bulk-alt cap) was already implemented
from the crawl sync spec — discovered and confirmed, no action needed.

## Accomplishments

1. **v2.14.4** — G-16 register_post_meta for all rr_seo_* keys (show_in_rest,
   type coercion, per-field sanitize); G-18 GET /canonical-urls/preview alias;
   G-13 rrseo_snippet_emitted / rrseo_snippet_skipped action hooks.

2. **v2.15.0** — G-10 POST /snippets/bulk (atomic batch create, rrseo_batch_max
   cap, per-item validation); G-09/G-17 GET+POST /sitemap/exclusions
   (excluded_post_slugs active now, term/taxonomy exclusions stored for future);
   expanded test placeholder patterns (do-not-index- prefix).

3. **v2.16.0** — G-04/G-05 Performance module: GET+POST /perf/dequeue-rules
   (wp_enqueue_scripts:999, when_not whitelist validated at write + apply time),
   GET+POST /perf/defer-handles (script_loader_tag defer injection). Admin title
   em-dash raw bytes fixed (ASCII hyphen). /status adds perf counts.

4. **v2.16.1** — G-19 description fallback in page head (excerpt then first
   paragraph via rr_get_discovery_description, skips title, rrseo_excerpt_fallback_length
   filter default 155); G-06 migrate-legacy skips %token% pattern fields
   (skipped_due_to_template_token reason + raw value surfaced).

5. **v2.17.0** — G-07 POST /elementor/repair-cache (clears _elementor_element_cache
   + _elementor_css, single or batch, reports deleted_keys); G-14 display_on_user
   field on all snippet write endpoints (all|anonymous|logged_in, emitter enforces,
   fires rrseo_snippet_skipped with user_logged_in/user_anonymous). Orphaned
   docblock in image handler section removed.

6. **README.md created** — REST API reference with endpoint examples, display_on
   vocabulary, headless self-update workflow (this session updated STARTUP_CONTEXT).

## Technical Changes (this sprint)

| Version | Files | Summary |
|---|---|---|
| v2.14.4 | rankmath-rest-bridge.php | register_post_meta, /canonical-urls/preview, snippet hooks |
| v2.15.0 | rankmath-rest-bridge.php, includes/class-rrseo-canonical.php | bulk snippets, sitemap exclusions, placeholder patterns |
| v2.16.0 | rankmath-rest-bridge.php | performance module (dequeue + defer), admin title fix |
| v2.16.1 | rankmath-rest-bridge.php | description fallback, migrate-legacy token guard |
| v2.17.0 | rankmath-rest-bridge.php | Elementor cache helper, display_on_user |

## Known Issues / Blockers

None. All gaps from the original gap report are closed except:
- G-15 (hreflang) — explicitly deferred, not started
- G-14 (per-user-state) — DONE in v2.17.0

## Next Session Priorities

1. **Validate v2.17.0 on live site** — install update, spot-check key new
   endpoints: /perf/dequeue-rules, /perf/defer-handles, /snippets/bulk,
   /elementor/repair-cache, display_on_user on a test snippet.

2. **Salvo staging verify** — install v2.17.0 on Salvo; configure
   /perf/dequeue-rules to replace RRC_SEO_WC_DEQUEUE mu-plugin module;
   configure /perf/defer-handles to replace RRC_SEO_DEFER_NONCRIT; verify
   term:product_cat:<slug> snippets fire (G-01 end-to-end WooCommerce).
   Then retire the two mu-plugin modules.

3. **rrc-mu-toolkit GitHub remote + retire sequence** — create remote, push.
   Retire RRC_SEO_TAX_META_DESC and RRC_SEO_TAX_META_OG on Salvo once
   Salvo staging verify confirms v2.17.0 is solid.

## Backlog Movement

CLOSED this session:
- G-13, G-16, G-18 (v2.14.4)
- G-10, G-09, G-17 (v2.15.0)
- G-04, G-05 (v2.16.0)
- G-06, G-19 (v2.16.1)
- G-07, G-14 (v2.17.0)
- Images polish (GET /images/{id}/alt, bulk-alt cap) — already done, confirmed

REMAINING OPEN:
- G-15 (hreflang) — DEFERRED
- Salvo staging verify
- rrc-mu-toolkit GitHub remote + retire sequence

## Git Status

- Branch: `main`
- Version: 2.17.0
- Last commit: `53b3a00` — chore: release v2.17.0 zip
- Pushed: yes
- Working tree: clean (after this checkpoint commit)
