# Checkpoint — 2026-05-14_1845

## Context Summary

v2.13.1 hotfix session. Three-state live-site validation of v2.13.0 revealed
that the headline features (G-01 taxonomy routing, G-02 term writes) were not
correctly functional despite being in the code. Implemented a gating-and-fix
release (v2.13.1), validated it, and confirmed G-01 emission is actually working
via `term_id:` probe. v2.14.0 scope is now well-defined.

## Accomplishments

1. **Reviewed v2.13.0 three-state validation report** — identified 6 unfixed gaps,
   2 high-severity silent failures (G-01 stored-but-no-emit, G-02 valid:true on
   term writes), 2 cosmetic whitespace bugs (leading space in title, double space
   in description).

2. **Shipped v2.13.1** — two fixes:
   - 422 gate on `term:`, `tax:`, `url:` `display_on` patterns at snippet
     create/update. `term_id:<int>` remains accepted. Both handlers updated;
     `accepted_patterns[]` and `hint` in error body. `rr_validate_display_on()`
     docstring updated.
   - `rmb_resolve_tokens()` whitespace normalization: `trim()` + `preg_replace`
     collapses `[ \t]+` to single space. Fixes leading space in `<title>` and
     double space in `<meta name="description">` after RankMath deactivation.
   - Version bumped 2.13.0 → 2.13.1. CHANGELOG.md entries added for both
     v2.13.1 (fix) and v2.13.0 (retroactive). Manifest updated. Zip built and
     pushed via `bin/build-zip.ps1`. Auto-update confirmed working on live site.

3. **Reviewed v2.13.1 validation report** — both fixes verified clean. Bonus:
   `term_id:21` snippet proven to fire on `/category/ai-search-marketing/` and
   not on homepage/single/page. G-01 emitter confirmed working. Full 8-pattern
   emission matrix documented.

## Technical Changes

| File | Change |
|---|---|
| `rankmath-rest-bridge.php` | v2.13.1: 422 gate, whitespace fix, docstrings |
| `update-manifest.json` | version 2.13.1, download_url, changelog entry |
| `CHANGELOG.md` | v2.13.1 and v2.13.0 entries added |
| `releases/v2.13.1/rankmath-rest-bridge.zip` | Release artifact |

Commits: `a2f5184` (fix), `24d2ae8` (zip) — both pushed to main.

## Known Issues / Open Gaps

| Gap | Status |
|---|---|
| G-01 `term:`, `tax:` emission | Gated (422). Emitter proven via term_id:. Lift in v2.14.0 |
| G-02 `/preview-update` + `/update` term IDs | Silent `valid: true` — must return 422 in v2.14.0 |
| G-03 `consolidate_canonical` flag | Missing from /status and /settings |
| G-08 `emit_routing_version` in /status | Field absent |
| G-11 GET /snippets/<slug> | 404 (collection works) |
| G-12 POST /llms-txt/regenerate | 404 |
| `post_id:<int>` alias | Dropped in v2.13.1 — restore or document in v2.14.0 |
| New patterns undocumented | `singular`, `all_pages`, `all_posts`, `post_type:<slug>` need README/CHANGELOG |

## Next Session Priorities

1. **v2.14.0 implementation** (in order):
   - Restore `post_id:<int>` alias in `rr_validate_display_on()`
   - Lift G-01 gate: add `term:`, `tax:` back to validator allowlist
   - G-02 fix: 422 in `/preview-update` and `/update` when ID resolves to term
   - G-03: `consolidate_canonical` flag in /status and /settings
   - G-08: `emit_routing_version: 2` in /status
   - G-11: GET /snippets/<slug> single-resource endpoint
   - G-12: POST /llms-txt/regenerate endpoint

2. **Salvo staging verify** — install v2.13.1; confirm 422 gate fires correctly.

3. **rrc-mu-toolkit** — create GitHub remote; retire RRC_SEO_TAX_META_DESC and
   RRC_SEO_TAX_META_OG constants on Salvo after staging confirm.

## Backlog Movement

- G-01 moved from "blocked" to "gate-ready for v2.14.0" (emitter proven)
- `post_id:` alias added to v2.14.0 scope (was untracked)
- Pattern documentation added to v2.14.0 scope

## Git Status

- Branch: `main`
- Last commit: `24d2ae8` — chore: release v2.13.1 zip
- Pushed: yes
- Working tree: clean
