# Checkpoint — 2026-05-25_1200

## Context Summary

Short housekeeping session. No feature work — two latent issues addressed
(duplicate route bug, missing WP compatibility metadata) and white-label
rename documented for client reference.

---

## Accomplishments

1. **v2.17.4 — duplicate route bug fixed** — `GET /canonical-urls/preview` was
   registered twice: the G-18 alias (line 2258, callback `rmb_sitemap_preview`,
   correct) and a stale AEO/GEO stub (line 2575, callback
   `rmb_canonical_urls_preview`, function never implemented). WordPress served
   the first match silently, making the undefined callback a latent fatal.
   Duplicate removed; G-18 alias is the sole authoritative registration.
   PHPCS clean. Committed `32696fc`, pushed.

2. **WordPress 7.0 compatibility declared** — added `Tested up to: 7.0` to
   plugin header; bumped `"tested"` in `update-manifest.json` from `6.7` to
   `7.0`. Committed `96f3dea`, pushed.

3. **White-label rename documented** — explained `RRSEO_WL_NAME` /
   `RRSEO_WL_DESCRIPTION` / `RRSEO_WL_AUTHOR` / `RRSEO_WL_AUTHOR_URL` /
   `RRSEO_WL_SUPPORT_URL` / `RRSEO_WL_HIDE_PLUGIN` constants to user; confirmed
   single-line rename (`define( 'RRSEO_WL_NAME', 'AMS SEO' )` in wp-config.php).
   No code changes.

---

## Technical Changes

| File | Change |
|------|--------|
| `rankmath-rest-bridge.php` | Removed duplicate `/canonical-urls/preview` route; added `Tested up to: 7.0` header; version bumped 2.17.3 -> 2.17.4 |
| `update-manifest.json` | version 2.17.4, download_url updated, tested 7.0, changelog entry prepended |
| `CHANGELOG.md` | v2.17.4 entry added |
| `docs/STARTUP_CONTEXT.md` | Accomplishments, version, commit, blockers updated |

---

## Known Issues / Blockers

None. v2.17.4 is ship-quality.

---

## Next Session Priorities

1. **Salvo staging verify** — install v2.17.4; configure `POST /perf/dequeue-rules`
   to replace `RRC_SEO_WC_DEQUEUE`; configure `POST /perf/defer-handles` to
   replace `RRC_SEO_DEFER_NONCRIT`; verify `term:product_cat:<slug>` snippets
   fire on WooCommerce taxonomy archives (G-01 end-to-end).

2. **rrc-mu-toolkit GitHub remote + retire sequence** — create remote, push.
   Retire mu-plugin modules in order: `RRC_SEO_DEDUP_CANONICAL` first (lowest
   risk), then tax-meta modules after G-01 WC verified, then perf modules after
   staging verify.

3. **G-14 logged-in emission manual check** — log in to WP admin on rankrocket.co,
   view front-end with a `display_on_user: logged_in` snippet active; confirm it
   fires for logged-in and suppresses for anonymous. Must be done manually
   (Basic Auth does not establish a WP session).

---

## Backlog Movement

None this session.

---

## Git Status

- Branch: `main`
- Last commit: `96f3dea` — chore: mark tested up to WordPress 7.0
- Pushed: yes
- Working tree: clean
