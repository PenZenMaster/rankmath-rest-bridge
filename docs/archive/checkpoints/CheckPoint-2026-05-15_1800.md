# Checkpoint — 2026-05-15_1800

## Context Summary

End-of-session checkpoint. Continued from the 1600 checkpoint. Worked through
three v2.17.x validation reports, shipping hotfixes for each. All validation
report findings are now resolved or characterized. Session closes with the
plugin at v2.17.3 and working tree clean.

## Accomplishments

1. **v2.17.2 shipped** — targeted WP object cache invalidation
   (`rrseo_bust_option_cache()` at all 8 update_option() sites); G-10 bulk
   slug collision fixed with _1/_2/_3 increment; FU-2 documented (unset_fields
   is the correct clear mechanism, empty string is intentional no-op).

2. **v2.17.3 shipped** — LiteSpeed page cache purge after writes
   (`rrseo_purge_rest_cache()` fires `litespeed_purge_url` for `/status` and
   `/snippets` after every write); G-10 individual POST /snippets slug
   collision fixed (while-loop increment matching bulk behavior).

3. **All validation reports committed** — v2.17.0, v2.17.1, v2.17.2 reports
   in repo. FU-2 (unset_fields) fully closed per v2.17.2 report.

4. **Architecture doc reviewed** — `docs/aeo_geo_google_data_architecture.md`
   confirmed as external audit engine spec, not plugin scope. Plugin's role
   (read-only data provider via existing REST endpoints) is already fulfilled.

5. **Mu-plugin retirement sequenced** — confirmed which modules can be retired
   (RRC_SEO_DEDUP_CANONICAL, RRC_SEO_TAX_META_DESC, RRC_SEO_TAX_META_OG,
   RRC_SEO_WC_DEQUEUE, RRC_SEO_DEFER_NONCRIT) and the required order:
   disable constant → Salvo staging verify → remove code → commit.

## Technical Changes

| Version | Key changes |
|---|---|
| v2.17.2 | rrseo_bust_option_cache() at all write sites; bulk slug _1/_2/_3; README unset_fields note |
| v2.17.3 | rrseo_purge_rest_cache() LiteSpeed URL purge; individual POST slug while-loop fix |

## Known Issues / Blockers

- **Cache-A/B** — fully addressed as of v2.17.3 (WP object cache bust + LiteSpeed URL purge)
- **G-10 batch cap** — 60 < 100 (working as designed, no fix needed)
- **G-13 observability** — /log endpoint exists, hooks not externally verifiable without a mu-plugin smoke test
- **G-14 logged-in verification** — still needs manual check (log in to WP admin, view front-end with a logged_in snippet)
- **G-15 hreflang** — deferred

## Next Session Priorities

1. **Salvo staging verify** — install v2.17.3; configure POST /perf/dequeue-rules
   to replace RRC_SEO_WC_DEQUEUE; configure POST /perf/defer-handles to replace
   RRC_SEO_DEFER_NONCRIT; verify term:product_cat:<slug> snippets fire on
   WooCommerce taxonomy archives (G-01 end-to-end). Then retire the two perf
   mu-plugin modules.

2. **rrc-mu-toolkit GitHub remote + retire sequence** — create GitHub remote,
   push. Retire RRC_SEO_DEDUP_CANONICAL (lowest risk), then RRC_SEO_TAX_META_DESC
   and RRC_SEO_TAX_META_OG once taxonomy snippet emission verified on Salvo.
   Order: disable constant → staging verify → remove code → commit.

3. **G-14 manual verification** — log in to WP admin on rankrocket.co, view
   front-end with a display_on_user=logged_in snippet active; confirm it fires
   for logged-in and suppresses for anonymous.

## Backlog Movement

CLOSED this session:
- FU-2 (unset_fields — confirmed working in v2.17.2 report)
- Cache-A/B (v2.17.2 + v2.17.3)
- G-10 slug collision (v2.17.2 bulk + v2.17.3 individual)

OPEN:
- Salvo staging verify
- rrc-mu-toolkit GitHub remote + retire sequence
- G-13, G-14, G-15 (deferred)
- FU-4 (global REST shutdown handler — quiet since v2.14.1)

## Git Status

- Branch: `main`
- Version: 2.17.3
- Last commit: `eac8587` — chore: release v2.17.3 zip
- Pushed: yes
- Working tree: clean
