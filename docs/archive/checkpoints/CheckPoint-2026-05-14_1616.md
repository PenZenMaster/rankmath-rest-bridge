# Checkpoint: 2026-05-14_1616

## Context Summary

Session focused on housekeeping and planning. No plugin code was changed.
Primary output: the gap report is now the single source of truth for the
roadmap, and v2.13.0 has a concrete implementation plan ready to execute.

---

## Accomplishments

1. **Scaffolded `rrc-mu-toolkit` repo** (`E:\projects\rrc-mu-toolkit`) — new
   standalone git repo for the `plugin-usage-audit.php` mu-plugin. Initial
   commit `4c7e017` includes the updated file, `.gitignore`, and `phpcs.xml.dist`.
   Refactor notes appended to the file header document the two hardcoded client
   values to parameterize (OG site suffix and fallback image URL), the class
   naming issue (`_V11`), and the separation-of-concerns split plan.

2. **Gap report committed and CSV retired** — `docs/RankRocket_SEO_Functionality_Gaps.md`
   is now the authoritative roadmap document (19 gaps, G-01 through G-19).
   `docs/Gap-Priority-Notes.csv` deleted (preserved in git history). G-06 summary
   table title corrected. 4 surviving CSV items migrated as G-16 through G-19.

3. **v2.13.0 planned** — full implementation plan for G-01 (taxonomy `display_on`
   patterns), G-02 (term meta read/write + taxonomy archive emission), and G-08
   (`display_on` validation). Five-commit sequence defined, risks identified,
   version bump strategy set (2.12.2 -> 2.13.0).

---

## Technical Changes

| Commit | Hash | Summary |
|---|---|---|
| chore: exclude plugin-usage-audit.php | 20dae31 | .gitignore updated |
| docs(gaps): add gap report, retire CSV | 04f6a88 | MD added, CSV deleted, G-06 fixed |
| docs(gaps): migrate G-16 through G-19 | a79b18a | 4 CSV items migrated into report |

All 3 commits pushed to `origin/main` (`474b421..a79b18a`).

`rrc-mu-toolkit` repo is local only — no remote configured yet.

---

## Known Issues / Blockers

- None.

---

## Next Session Priorities

1. **Implement v2.13.0 — Step 1 (G-01):** Add `rr_is_any_tax_archive()` helper
   and extend `rmb_snippet_matches_display()` for `term:TAX:SLUG`, `term_id:NNN`,
   `tax:TAX`, and `url:/path/` patterns (~lines 371-410 of `rankmath-rest-bridge.php`).

2. **Implement v2.13.0 — Step 2 (G-08):** Add `rr_validate_display_on()` and wire
   422 validation into `rmb_snippets_create()` (~line 3011) and `rmb_snippets_update()`
   (~lines 3042-3046).

3. **Implement v2.13.0 — Step 3 (G-02):** `rr_resolve_id()` helper, term meta
   read/write routing in `/update` and `/get/{id}`, taxonomy archive emission on
   `wp_head` + `pre_get_document_title`.

---

## Backlog Movement

- No backlog changes this session. Gap report is now the backlog.

---

## Git Status

- Branch: `main`
- Last commit: `a79b18a` — pushed
- Working tree: clean
- `rrc-mu-toolkit`: local only, commit `4c7e017`
