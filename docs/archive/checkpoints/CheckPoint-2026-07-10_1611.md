# CheckPoint 2026-07-10_1611 -- Shutdown: 4 releases, v3.0 roadmap done, #7 + #8 shipped

## Context Summary

Shutdown checkpoint for the 2026-07-10 session (third checkpoint today).
After the morning's v3.0.0 breaking release and the afternoon's roadmap
completion (Bites 3+4), the user filed two field-driven feature requests
from live Higgins perf work; both were built, shipped, and closed
same-day (v3.2.0 snippet priority, v3.3.0 WAF-safe base64 transport).

## Accomplishments (whole session)

1. **v3.0.0** -- replace-all endpoint removed (breaking; user signed off
   after business-facing impact breakdown); capability auto-revoked on
   upgrade. Deployed + verified on Higgins Overhead Door.
2. **v3.1.0 (Bite 3)** -- rollback layer: GET /actions/{action_id} +
   POST /actions/{action_id}/rollback (drift detection with force
   override, double-rollback protection, dry-run). Deployed +
   smoke-tested.
3. **CI (Bite 4)** -- .github/workflows/ci.yml: phpcs + phpunit on PHP
   7.4 + 8.3 plus release-integrity job. composer platform.php pinned
   7.4.33 (defused doctrine/instantiator ^8.4 landmine). Green on every
   push since.
4. **v3.2.0 (issue #7)** -- snippet priority field (0-10000 -> WP hook
   priority); one emitter per (location, priority) bucket; per-location
   defaults preserved.
5. **v3.3.0 (issue #8)** -- code_b64 WAF-safe transport on all three
   snippet write endpoints; strict base64 + UTF-8 validation.
6. **Issues #5-#8 closed**; playbook shell-labeling rule added; suite
   grew 179/408 -> 207/511; first unit coverage of snippet emission path.

## Technical Changes (since 13:59 checkpoint)

| File | Change |
|---|---|
| `rankmath-rest-bridge.php` | Emission rewrite (bucket registrar, priority-aware renderer), 3 write handlers gained priority + code_b64, helpers rmb_snippet_priority / rr_validate_snippet_priority / rr_decode_snippet_b64; v3.3.0 |
| `tests/unit/SnippetPriorityTest.php` | New: 9 tests |
| `tests/unit/SnippetBase64Test.php` | New: 6 tests |
| `tests/bootstrap.php` | is_user_logged_in, esc_attr stubs |
| `README.md` | priority + code_b64 docs, WP priority reference table |
| `releases/v3.2.0/`, `releases/v3.3.0/` | Zips (hook-built, 4/4 checks) |

Commits since morning: 98c56e9, 70a45e2, ab18857, b42cdcf, cd40ebb,
e22873b, 3b84d03, 335e8e5, 30788c2, d60a1a3, 061dbe6, fd01178.

## Known Issues / Blockers

- None. All gates green (phpcs, 207/511 phpunit, CI 3/3 jobs).
- Higgins still on v3.2.0; v3.3.0 zip on CDN awaiting user-run
  self-update.
- Live execute->rollback cycle on production not explicitly confirmed.

## Next Session Priorities

1. Deploy v3.3.0 to Higgins; POST the render-block swap snippet
   (code_b64 + priority:1); measure PageSpeed (expect mobile 63 -> 78-85).
2. Telemetry verdict review (~2026-07-13; RRC_PUA_DISABLE kill switch).
3. Cleanup: live rollback confirmation, llms.txt raw-content
   verification, stale bite checkboxes in projectStatus.md.

## Backlog Movement

- Closed: v3.0 Bites 3+4 (roadmap complete); issues #7, #8
- Added + closed same day: snippet priority, WAF-safe transport
- Unchanged: llms.txt raw-content verification, Future/Deferred list

## Git Status

- Branch: `main`, in sync with origin
- Last commit at checkpoint: `fd01178`
- Pushed: yes
