# Checkpoint — 2026-05-14_1920 (Shutdown)

## Context Summary

Full session: reviewed 3-state v2.13.0 validation, shipped v2.13.1 hotfix,
validated it, then implemented and shipped v2.14.0 with all 7 follow-up items
from the validation reports. Session ends clean — two versions shipped, both
lint-clean, both pushed to GitHub with release zips.

## Accomplishments

1. **v2.13.1 shipped** — 422 gate on `term:`, `tax:`, `url:` `display_on` patterns;
   `rmb_resolve_tokens()` whitespace normalization. Validated on live site.
   G-01 emitter proven working via `term_id:21` live probe on category archive.

2. **v2.14.0 shipped** (7 items):
   - G-01 gate lifted: `term:` and `tax:` now accepted in validator + emitter
   - `post_id:` alias restored (dev spec T-02)
   - G-02: `/preview-update` returns 422 for term IDs
   - G-03: `consolidate_canonical` bool in `/status`
   - G-08: `emit_routing_version: 2` in `/status`
   - G-11: `GET /snippets/<slug>` single-resource endpoint
   - G-12: `POST /llms-txt/regenerate` endpoint

3. **Validation reports committed** — both v2.13.0 and v2.13.1 reports added to
   `docs/` as permanent record.

4. **Checkpoints and docs kept current** — `STARTUP_CONTEXT.md`,
   `projectStatus.md`, and checkpoint files updated throughout session.

## Technical Changes

| File | Change |
|---|---|
| `rankmath-rest-bridge.php` | v2.13.1 fixes; v2.14.0 features (7 items) |
| `update-manifest.json` | v2.14.0 version, URL, changelog |
| `CHANGELOG.md` | v2.14.0, v2.13.1, v2.13.0 entries |
| `releases/v2.13.1/rankmath-rest-bridge.zip` | Release artifact |
| `releases/v2.14.0/rankmath-rest-bridge.zip` | Release artifact |
| `docs/RankRocket_v2_13_0_Validation_Report.md` | Committed to repo |
| `docs/RankRocket_v2_13_1_Validation_Report.md` | Committed to repo |
| `docs/STARTUP_CONTEXT.md` | Refreshed for v2.14.0 |
| `docs/projectStatus.md` | Session 3 block added |
| `docs/archive/checkpoints/CheckPoint-2026-05-14_1845.md` | Mid-session checkpoint |

Key commits: `a2f5184` (v2.13.1 fix), `24d2ae8` (v2.13.1 zip),
`e7cf449` (checkpoint), `6e592a0` (v2.14.0 feat), `b71631b` (v2.14.0 zip)

## Known Issues / Open Gaps

| Gap | Status |
|---|---|
| G-01 `url:` routing | Still gated — emitter code present, live validation pending |
| G-02 term dry-run | `/preview-update` returns 422 for terms; no dry-run support yet |
| G-04 through G-07 | Not yet scoped |
| G-09, G-10 | Not yet scoped |
| G-13 through G-15 | Not yet scoped |
| G-16 register_post_meta | Deferred from v2.14.0 |
| G-17 | Not yet scoped |
| G-18 /canonical-urls/preview alias | Deferred from v2.14.0 |
| G-19 | Not yet scoped |

## Next Session Priorities

1. Validate v2.14.0 on live site — install update, spot-check G-11, G-12,
   `/status` new fields, and `term:` snippet acceptance.
2. Salvo staging verify — G-01 end-to-end on WooCommerce site with
   `term:product_cat:<slug>` snippets.
3. rrc-mu-toolkit GitHub remote + retire `RRC_SEO_TAX_META_DESC` /
   `RRC_SEO_TAX_META_OG` on Salvo.

## Git Status

- Branch: `main`
- Last commit: `b71631b` — chore: release v2.14.0 zip
- Pushed: yes (after shutdown commit)
- Working tree: clean (after shutdown commit)
