# Checkpoint — 2026-05-15_1330

## Context Summary

Mid-session checkpoint. Started with v2.14.0 shipped and clean. Reviewed the
v2.14.0 validation report (6/7 pass, G-12 fatal), then iterated through
v2.14.1, v2.14.2, and v2.14.3 closing all follow-up items from both the
v2.14.0 and v2.14.1 validation reports.

## Accomplishments

1. **Committed v2.14.0 validation report** — 6/7 pass, G-12 fatal (PHP 500)
   flagged with root cause and reproduction steps.
2. **v2.14.1 shipped** — G-12 hotfix: `rmb_llms_regenerate()` passed the
   array returned by `rr_render_llms_txt()` directly to `substr_count()` /
   `strlen()` (both require a string). Fixed by unpacking `$result['content']`.
3. **v2.14.1 validation report reviewed and committed** — G-12 confirmed
   fixed (200 on all 4 payload variants, deterministic md5). Off-by-one in
   `line_count` noted. Headless `/self-update` flow discovered and flagged.
4. **v2.14.2 shipped** — FU-2: added `unset_fields` array parameter to
   `POST /update`. Explicit meta deletion for posts and terms. Returns 422
   on unknown fields or write/unset conflicts. Audited.
5. **v2.14.3 shipped** — FU-1b/FU-4/FU-3/FU-5:
   - FU-1b: `line_count` off-by-one fixed (drop spurious `+1`)
   - FU-4: `rrseo_rest_fatal_handler()` shutdown function — catches PHP fatals
     during REST requests, emits clean JSON 500, prevents HTML leak
   - FU-3/FU-5: `README.md` created; `/update` term support and headless
     self-update workflow (`/check-updates` + `/self-update`) documented

## Technical Changes

| File | Change |
|---|---|
| `rankmath-rest-bridge.php` | v2.14.1: unpack `$result['content']` in `rmb_llms_regenerate` |
| `rankmath-rest-bridge.php` | v2.14.2: `unset_fields` param + handler logic in `rmb_update_meta` |
| `rankmath-rest-bridge.php` | v2.14.3: `line_count` fix; `rrseo_rest_fatal_handler` added |
| `CHANGELOG.md` | v2.14.1/2/3 entries |
| `update-manifest.json` | version + download_url + changelog entries through v2.14.3 |
| `README.md` | Created — REST API reference, display_on vocab, self-update workflow |
| `releases/v2.14.1–3/` | Zips built and committed |
| `docs/RankRocket_v2_14_0_Validation_Report.md` | Committed (was untracked) |
| `docs/RankRocket_v2_14_1_Validation_Report.md` | Committed |

## Known Issues / Blockers

None. All FUs from v2.14.0/v2.14.1 reports closed.

## Next Session Priorities

1. **Salvo staging verify** — install v2.14.3 on Salvo; confirm
   `term:product_cat:<slug>` snippets fire on WooCommerce taxonomy archives
   (G-01 end-to-end, first live WC test).
2. **rrc-mu-toolkit GitHub remote + retire sequence** — create remote, push.
   Then retire `RRC_SEO_TAX_META_DESC` and `RRC_SEO_TAX_META_OG` on Salvo:
   disable constant → staging verify → remove code → commit.
3. **Choose next gap to implement** from the open list:
   G-10 (bulk snippet POST) and G-09 (sitemap term exclusion) are highest
   practical value. G-13 (snippet emission hooks) is low effort.

## Backlog Movement

- FU-1b, FU-2, FU-3, FU-4, FU-5: all moved to DONE
- Open gap list unchanged: G-04, G-05, G-06, G-07, G-09, G-10, G-13, G-14
- G-15 (hreflang): still deferred

## Git Status

- Branch: `main`
- Version: 2.14.3
- Last commit: `97cb348` — chore: release v2.14.3 zip
- Pushed: yes
- Working tree: clean (after this checkpoint commit)
