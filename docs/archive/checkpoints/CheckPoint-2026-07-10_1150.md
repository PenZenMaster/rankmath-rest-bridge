# CheckPoint 2026-07-10_1150 -- v3.0.0 Breaking Release Shipped + Deployed

## Context Summary

Session goal was to resolve the deferred replace-all decision from the
v2.19.0 CHANGELOG. User signed off on removal after reviewing a
business-facing impact breakdown; the breaking release shipped, deployed,
and verified in production the same session. Bite 3 (rollback layer) work
begins immediately after this checkpoint.

## Accomplishments

- **replace-all removal SIGNED OFF + SHIPPED (v3.0.0)** -- user approved
  after a business-team impact breakdown. `POST /snippets/replace-all`
  route, `rmb_snippets_replace_all()` handler, and `RR_REPLACE_ALL_CAP`
  constant removed. Capability provisioning hook inverted to a cleanup
  hook: revokes `rrseo_replace_all_snippets` from administrator on first
  load (grant was persisted in the DB with the role).
- **v3.0.0 DEPLOYED TO PRODUCTION** -- Higgins Overhead Door, via
  self-update jumping 2.18.1 -> 3.0.0 (v2.19.0 was never separately
  deployed). User reports no issues.
- **Issue #5 VERIFIED + CLOSED** -- action-engine smoke test passed on the
  live install (dry-run + execute for update_setting blog_public launch
  flow and toggle_indexing). Verification comment added to the closed
  issue.
- **Playbook rule added (committed `ab18857`)** -- every command/script
  given to the user must be labeled with its execution environment
  (PowerShell/CMD/Git Bash/SSH). Codified in global CLAUDE.md section 6,
  project playbook section 0, and auto-memory.

## Technical Changes

| File | Change |
|---|---|
| `rankmath-rest-bridge.php` | Removed replace-all route + handler + cap constant; cap-cleanup hook; version 3.0.0 |
| `update-manifest.json` | 3.0.0, download_url -> releases/v3.0.0/, BREAKING changelog entry |
| `CHANGELOG.md` | v3.0.0 entry (removal, migration paths, rationale); v2.19.0 deferred note marked signed off |
| `README.md` | replace-all section -> removal notice; documented /snippets/bulk + DELETE /snippets/{slug} |
| `.claude/claude.md` | Stale capability guardrail -> "no bulk wipe-and-replace endpoints"; shell-labeling rule |
| `releases/v3.0.0/` | Zip built by pre-push hook (128 entries, 4/4 structural checks) |

Commits: `98c56e9` (feat!: removal), `70a45e2` (release zip),
`ab18857` (docs: shell-labeling rule).

## Known Issues / Blockers

- None. Gates green: phpcs clean (9 files), phpunit 179 tests / 408
  assertions.
- Removal spot-check curl (expect `not_found` 404 from the {id} wildcard,
  NOT `rest_no_route` -- the wildcard catches "replace-all" as a slug) was
  written for the user but not confirmed run. Non-blocking; production
  running clean.
- Git index quirk discovered: playbook tracked as `.claude/claude.md`
  (lowercase); `git add` with uppercase path silently stages nothing on
  Windows.

## Next Session Priorities

1. **v3.0 Bite 3 -- rollback layer (STARTED this session)** --
   `GET /actions/{action_id}` reading `rrseo_action_log`, then
   `POST /actions/{action_id}/rollback` replaying envelopes. Edge cases:
   `reversible: false` actions, double rollback, current-value drift since
   execution.
2. **Bite 4 -- GitHub Actions CI** (phpcs + phpunit on push/PR).
3. **Telemetry verdict review** -- rrc-telemetry.php collecting since
   2026-07-06; trustworthy from ~2026-07-13.

## Backlog Movement

- Closed: replace-all removal decision (shipped v3.0.0)
- Closed: issue #5 (verified in production)
- Started: Bite 3 rollback layer
- Unchanged: llms.txt raw-content upload verification

## Git Status

- Branch: `main`, in sync with origin
- Last commit at checkpoint: `ab18857`
- Pushed: yes
